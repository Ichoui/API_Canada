{
  "apps": [
    {
      "name": "API",
      "script": "app.js",
      "env": {
        "COMMON_VARIABLE": "true"
      },
      "env_production": {
        "NODE_ENV": "production"
      }
    },
    {
      "name": "WEB",
      "script": "web.js"
    }
  ],
  "deploy": {
    "production": {
      "user": "root",
      "host": "[176.158.217.80, 127.0.0.1]",
      "port": "4620",
      "ref": "origin/master",
      "repo": "git@gitlab.com:Ichoui/API_Canada.git",
      "path": "/var/www/API",
      "ssh_options": "StrictHostKeyChecking=no",
      "pre-setup": "apt-get install git",
      "post-setup": "ls -la",
      "pre-deploy-local": "echo 'This is a local executed command'",
      "post-deploy": "npm install && pm2 startOrRestart ecosystem.json --env production",
      "env": {
        "NODE_ENV": "production"
      }
    }
  }
}